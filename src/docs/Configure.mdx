import { Meta } from '@storybook/blocks'

<Meta title="Comece com Aurea-UI" />

# 📦 Aurea-UI

**Aurea-UI** é uma biblioteca de componentes acessíveis para Vue 3, com foco em design moderno, minimalista e responsivo. Foi criada com o objetivo de facilitar a criação de interfaces bonitas e funcionais com foco em acessibilidade (WCAG AA).

## ✨ Destaques

- Componentes acessíveis e responsivos
- Design elegante e minimalista
- Fácil integração via NPM
- Compatível com screen readers (NVDA, VoiceOver)
- Customização com SCSS e props

## 📦 Instalação

```bash
npm install aurea-ui
```

## 🚀 Uso global no Vue

```ts
// main.ts
import { createApp } from 'vue'
import App from './App.vue'

import AureaUI from 'aurea-ui'
import 'aurea-ui/dist/index.css'
import '@fortawesome/fontawesome-free/css/all.min.css'

app.use(createPinia())
app.use(AureaUI)
app.mount('#app')
```

## 📚 Componentes disponíveis

- `AButton`
- `AInput`
- `AInputPassword`
- `ALogin`
- `ARegister`
- `AModal`
- `ANavbar`
- `ACard`
- `ANotification`
- `ALoading`

## 🧪 Acessibilidade

Todos os componentes foram desenvolvidos com foco em acessibilidade para pessoas com deficiência visual, garantindo suporte completo a leitores de tela, navegação por teclado, foco e uso adequado de labels e atributos ARIA.

## 📘 Navegue

Use a barra lateral para visualizar e testar cada componente com seus respectivos controles e exemplos.
